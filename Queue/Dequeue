#include<stdio.h>
#define MAX 5
int queue[MAX];
int front=0,rear=-1;
int main()
{
    int n;
    printf("enter the n elements(MAX %d):",MAX);
    scanf("%d",&n);
    if(n>MAX)
    {
        printf("queue overflow");
        return 0;
    }
    for(int i=0;i<n;i++)
    {
      scanf("%d",&queue[i]);
      rear++;
    }
    if(front>rear)
    {
        printf("queue underflow");
    }
    else
    {
        printf("deleted element:%d\n",queue[front]);
        front++;
    }
    printf("after deletion:");
    if(front>rear)
    {
        printf("queue overflow");
    }
    else
    {
        for(int i=front;i<=rear;i++)
        {
            printf("%d ",queue[i]);
        }
    }
    return 0;
}
